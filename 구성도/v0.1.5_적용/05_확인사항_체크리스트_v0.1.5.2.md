# 확인사항 체크리스트 v0.1.5.2

> **기준**: [00_v0.1.5.1_설계서.md](00_v0.1.5.1_설계서.md) + [06_논리적_오류_정의.md](06_논리적_오류_정의.md)
> + 그 이후 확정된 모든 결정사항
> **이전 버전**: [05_확인사항_체크리스트.md](05_확인사항_체크리스트.md) (v0.1.5.1 시점) — 이력 보존용
> **작성 시점**: 2026-07-26

## 이번 버전에서 달라진 것

| 구분 | 내용 |
|---|---|
| 해소 | 2순위 7개 중 **5개 완전 해결**, 2개 부분 해결 |
| 해소 | 3순위 O-10·O-11 해결, 4순위 **O-2는 D-02 결정으로 자동 소멸** |
| **신규 편입** | **High 등급 결함 5건(D-04~D-08)이 이전 체크리스트에 누락되어 있었음** — 이번에 정식 편입 |
| 신규 포착 | 논리오류 26종 정의의 프롬프트 공급 방식, 3축 심각도 판정의 신뢰성 |
| **추가 편입 (2026-08-02)** | **재워크스루 결함 RD-01~RD-07을 C-2 섹션으로 편입** — 전량 해소 완료. 이 문서가 작성된 뒤 2차 워크스루가 실시되어 생긴 공백이었다 |

> ⚠ **이전 체크리스트의 누락**: v0.1.5.1 체크리스트는 Critical 3건만 다루고 High 5건을 아예
> 빼먹었다. [03_결함기록부.md](03_결함기록부.md)에는 계속 기록되어 있었으나 체크리스트로
> 올라오지 않아 시야에서 사라진 상태였다. 이번 버전에서 **C 섹션으로 정식 편입**한다.

---

## A. 결정 완료 — 조치 불필요 (이력용)

| 항목 | 결정 내용 |
|---|---|
| Platform 통신 카탈로그 | "50종" 상한 폐기 → 프레임워크만 고정, 무제한 관측 + 통계 품질관리 |
| 최신화팀 우선순위 | 5단계 → **4단계** (완전일치·최신 / 부분일치 / 간접참고 / 아카이브·불확실) |
| 논리오류 반려 임계 | **3축 심각도**(핵심성×파괴성×수용가능성). 기만·왜곡은 무조건 반려, 그 외는 조합 판정 |
| Research 거버넌스 | 경로(Taxonomy 경유/스탠드얼론)와 무관하게 검증·승인 게이트 동일 적용 |
| 전처리부 스킬 저장소 | `skills/preprocess/<type>/` 파일형식별 하위폴더 구조 채택 |
| Ingestion 크롤링 | 실제 크롤링 없음 — 사용자 제공 원문만 처리 (오프라인 전제 유지) |
| SalesCS 범위 | 좁은 범위(리포트 포맷·전달 + 후속 질의응답), 외부 B2B 영업 제외 |
| 재작업 루프 상한 | 3회 초과 시 미결작업.md 이관 + 부분 리포트 |
| 미결작업 재개 | 자동화 없음 — 사람이 직접 확인 후 재요청 |
| 검증 Unverifiable 구분 | 별도 분기 없이 `note` 필드에 서술로만 기록 |
| Taxonomy 1단계 | **한국표준산업분류(KSIC) 대분류 18개 섹션** — 공식 11차 21개(A~U) 중 O·T·U(비사업 부문) 제외. **기준 차수 11차 통일 + 공식 문서 대조 검증 완료**(D-14, 2026-07-26). 10차 입력은 변환 후 원본 차수 보존 |
| Taxonomy 2단계 | **기업**. 1단계를 **복수로 가질 수 있으며 매출 비중 가중치 포함**(`company_industries: Vec<WeightedIndustry>`) + **비중 1위를 `primary_industry`로 자동 지정**(O-42) |
| Taxonomy 3단계 | **분류가 아니라 "옵션"** — **14개 그룹**의 속성 태그 집합([07_기업_옵션_정의.md](07_기업_옵션_정의.md)). 그룹별 단일/복수 선택, 해당 없으면 비워두는 것이 정상. *(O그룹 세목 제외로 15→14, 재워크스루 정합성 정정)* |
| 라우팅 판단 주체 | **이사장**(`chairman::decide_routing`) — Taxonomy/Research 소속 아님 |

---

## B. Critical 3건 — ✅ **전량 구현 완료** (2026-08-02)

| ID | 결함 | 확정된 조치 | 상태 |
|---|---|---|---|
| **D-01** | 큐 재진입 교착 | `call_llm()` 헬퍼 하나만 `run_queued`를 호출하도록 강제. 조율 함수는 직접 호출 금지 | ✅ **구현 완료 (2026-08-02)** — `queue.rs`에 `call_llm()` 신설, 직접 호출 7곳 전부 경유 전환 + **`run_queued`를 private으로 강등**(다른 모듈은 컴파일 불가 → 관례가 아니라 컴파일러 강제). `cargo check` 경고 0 |
| **D-02** | 위키 필드 추가 시 데이터 소실 | `WikiEntry`에 `#[serde(default)]` 적용 (마이그레이션·백업 로직은 불필요로 결정) | ✅ **구현 완료 (2026-08-02)** — 구조체 수준 `#[serde(default)]` + `Default` derive. 구스키마 로드 회귀 테스트 통과. `Confidence`/`RelationKind` `Default`는 타입 추가 시점 작업 |
| **D-03** | 질의 답변 자동 승격 → 자기오염 | 2저장소 분리: `wiki/draft.json`(미승격, 컨텍스트 조회 대상 아님) / `wiki/index.json`(승격) | ✅ **구현 완료 (2026-08-02)** — `DraftState` 신설 + `save_draft`/`list_draft`/`promote`/`discard_draft`. `query(wiki, draft, ..)`가 **서로 다른 타입**으로 두 저장소를 받아 호출부에서 뒤바꿀 수 없음. IPC 3종 + UI "📝 검토 대기"(반영/버리기)까지 반영. `promote`는 **ID를 유지**(관계·버전 체인이 ID를 가리키므로) |

### Critical 3건 구현 요약

| 검증 | 결과 |
|---|---|
| `cargo check` | ✅ 통과, **경고 0** |
| `cargo test wiki::tests` | ✅ **3/3 통과** — `new_id_unique_in_tight_loop`(D-06) · `wiki_entry_tolerates_missing_fields`(D-02) · `take_entry_removes_once_and_preserves_id`(D-03) |
| `dart analyze` | ✅ No issues found |
| `dart compile wasm` | ✅ 성공 |

> **D-01은 조치안보다 한 걸음 더 갔다**: `run_queued`를 `pub`에서 private으로 강등해 `call_llm`
> 외의 경로는 **컴파일 자체가 불가능**해졌다. "헬퍼 하나만 호출하도록 강제"가 관례가 아니라
> 타입 시스템의 보장이 된다 — D-03의 `query(wiki, draft, ..)` 타입 분리와 같은 방식이다.

---

## C. High 5건 — ✅ **전량 구현 완료** (2026-08-02)

> 이 5건은 [03_결함기록부.md](03_결함기록부.md)에 계속 기록되어 있었으나 체크리스트에 반영되지
> 않았던 항목이다. Critical만큼 즉각적이진 않지만 **전부 "동작은 하는데 결과가 조용히 틀리는"**
> 유형이라 사용자가 오류를 인지할 수 없다.
>
> **2026-08-02 전량 구현 완료.** 순서는 D-06+D-07 → D-02 → D-01 → D-03 → D-08①②③ →
> **필드 확장** → **OpenDART 연동** → D-05(b→c→a) → D-08④ → D-04. 중간의 필드 확장·DART
> 연동은 계획에 없던 선행 작업으로, D-05c의 정규화 키가 `corp_code`라 우회할 수 없었다.

| ID | 결함 | 현재 상태 | v0.1.5.1 설계와의 관계 |
|---|---|---|---|
| **D-04** | 컨텍스트 임계값 all-or-nothing (`wiki.rs:244` 6000자) — 재무 수치가 조용히 미전달 | ✅ **구현 완료 (2026-08-02)** | `preflight.rs` 신규 — `ContextBudget::from_num_ctx`(11 §4 수식, 상수 미사용) + 4단계 티어 랭킹(LLM 없음) + `allocate_budget`(09 §3.3). **`body_required` 페이지는 요약 강등 없이 제외하고 답변에 명시**, 일반 페이지만 요약 강등. `query()`가 프리플라이트 경유로 전환(§8.2.2). 회귀 테스트 6건 통과. ⚠ O-22(`body_required`를 누가 자동으로 세우는가)는 미결이라 현재는 작성자가 표시 |
| **D-05** | company 스코프 필터 부재 — 타 기업 데이터 혼입 | ✅ **구현 완료 (2026-08-02)** | 선결인 **OpenDART 연동(`dart.rs`)을 먼저 구현**해 `corp_code`를 확보한 뒤 **b→c→a 순서대로** 전부 반영. `list_scoped`는 `corp_code` 일치 **또는 개념 페이지(`company == None`)** 통과, `query`·`lint` 양쪽 스코프 지원, 미지정 시 "전체 대상" 응답에 명시. **D-14 lint 오탐도 함께 해소**(줄마다 기업명 병기 + 프롬프트 명시). 회귀 테스트 3건 통과 |
| **D-06** | `new_id()` 밀리초 정밀도 → ID 충돌 | ✅ **구현 완료 (2026-08-02)** | 확정 조치안 그대로 반영 — `SEQ: AtomicU64` 단조 카운터로 `wiki_<ts>_<seq>` 생성 + **ID 생성을 락 안으로 이동**(생성 순서 = 삽입 순서). 회귀 테스트 통과: 1만 개 타이트 루프(0.03초 = 기존 충돌 조건) 전부 고유. `cargo check` 통과 |
| **D-07** | `index.json` 비원자적 전량 덮어쓰기 | ✅ **구현 완료 (2026-08-02)** | 확정 조치안 그대로 반영 — `index.json.tmp`에 완전히 쓴 뒤 `fs::rename`으로 원자적 교체(Windows에서도 기존 대상 교체). 중단 지점과 무관하게 파일은 항상 완결 상태. `load_entries`의 `unwrap_or_default`는 D-02 결정대로 유지. D-06과 한 작업으로 처리 |
| **D-08** | `parse_structured` — `BODY:` 정확 일치 요구, 본문 조용히 빈 값 | ✅ **구현 완료 (2026-08-02)** | **①②③**: 빈 본문/제목 → `ParseError` 명시적 실패, 파싱 완화(trim·대소문자·마크다운 장식·**`BODY:` 같은 줄 편입**), 3회 재시도 후 `미결작업.md` 이관, `"제목 없음"` 폴백 제거. **④**: 필드 확장·D-05로 축이 생긴 뒤 **기업·기간 2축** 구현 — `canonical_title()`이 유일한 제목 표현 경로, 그룹 전체 소급 적용, 구분 불가 시 중복 신고. 산업 축은 `AXES`에 한 항목 추가로 편입. 회귀 테스트 8건 통과 |

---

## C-2. 재워크스루 결함 (RD 계열) — **전량 해소** (2026-08-02 편입)

> **이 섹션이 왜 생겼나**: v0.1.5.1을 대상으로 한 **2차 워크스루**([13](13_재워크스루_결과.md))에서
> 신규 결함 7건(RD-01~RD-07)이 나왔는데, 이 체크리스트는 그 이전에 작성되어 **RD 계열이 아예
> 없었다.** 1차 때 High 5건이 누락됐던 것(위 ⚠ 참고)과 **같은 상황**이 반복될 뻔했으므로 정식
> 편입한다. 결함 정의의 단일 출처는 계속 13 문서이며, 여기서는 상태만 추적한다.

| ID | 심각도 | 결함 | 확정된 조치 | 상태 |
|---|---|---|---|---|
| **RD-04** | High | `run_governance_pipeline` **내부 미정의** — 파이프라인에서 가장 복잡한 구간이 호출 한 줄로만 존재 | [설계서 §2.5](00_v0.1.5.1_설계서.md) 신설. 검증 2종 병렬·게이트 집계·재작업 3회·미결 이관·부분 리포트·취소 확인 전부 기술. **§2.4 호출부 바로 뒤에 배치** | ✅ 해소 |
| **RD-01** | High | 프리플라이트·Taxonomy **순환 의존** | 설계서 §8.2.3 — **사전 매칭 + 재순위**. 위키 기업명 사전 매칭(LLM 없음)으로 힌트 확보, 실패 시 **1단계를 비운다**. 경로 A는 Taxonomy 후 재순위, 경로 B도 사전 매칭으로 함께 해소 | ✅ 해소 |
| **RD-02** | Medium | 확정 지표(성장성 3종)가 **미결 O-35-1에 종속** | `Refresh` = **"기간이 다른 레코드 추가"** 확정. `ChangeKind` 유효 정의를 D3(이력용) → 설계서 §8.1로 이관 | ✅ 해소 |
| **RD-03** | Medium | Guard 1이 **존재하지 않는 지표(생산성)를 방어** | **생산성 축을 6번째로 추가** — 재무진단 **6축 18종**([09 §2](09_재무수치_정의.md)) | ✅ 해소 |
| **RD-05** | Medium | `DocumentTags` **저장 위치 미정** | 설계서 §8.4 — `WikiEntry.doc_tags`, 자유 태그와 별도 칸 | ✅ 해소 |
| **RD-06** | Low | `CreditRiskSummary` **저장 위치 미정** | 설계서 §8.4 — 기업 페이지에 인라인, 별도 저장소 안 만듦. 분리 신호 2개는 Phase 2 | ✅ 해소 |
| **RD-07** | Low | 취소 후 `run_lock` 점유 → 자원 해제 지연 | 설계서 §5.4 — 알림 문구에 "진행 중 작업 완료 후 중단" 명시 | ✅ 해소 |

### RD 조치로 늘어난 문서 (구현 착수 전 확인 대상)

| 절 | 내용 | 성격 |
|---|---|---|
| 설계서 **§2.5** | 거버넌스 파이프라인 전체 | 신설 — 3차 트레이스에서 TD 8건 발견 → **전량 해소·반영**([14](14_신설절_트레이스.md)) |
| 설계서 **§8.2.3** | 기업 힌트 | 신설 — TD 2건 발견 → **해소·반영** |
| 설계서 **§8.4** | 위키 저장 구조 보강 | 신설 — TD 2건 발견 → **해소·반영** (원자적 패키지 소절 추가) |
| 설계서 §8.1 | `ChangeKind` 재정의 | 이관 — 3차에서 이상 없음 |
| 09 §2 | 생산성 축 3종 | 확장 — 3차에서 이상 없음 |

> ~~⚠ 이 3개 신설 절은 워크스루를 거치지 않았다~~ → **3차 트레이스 실시 완료(2026-08-02)**.
> 결과는 아래 C-3.

---

## C-3. 3차 트레이스 결함 (TD 계열) — ✅ **전량 해소** (2026-08-02)

> [14_신설절_트레이스.md](14_신설절_트레이스.md)가 **TD-01~TD-12의 단일 출처**(조치 현황은 14 §6).
> 함정 #14("한 번 펼쳤다고 바닥이 아니다")가 적중한 결과 — RD-04를 펼치며 채운 §2.5 안에서
> 다시 8건이 나왔고, 당일 전부 조치했다.

| 구분 | 내용 | 상태 |
|---|---|---|
| **High 2건** | **TD-01** 부분 리포트가 최종 회차 미승인 단위를 버전화 → **이번 회차 Approve만 버전화**로 정정(사용자 확정) / **TD-02** §8.4 WikiEntry 목록 필드 누락 → 3필드 복원 + **"유효본 선언은 원자적 패키지"** 소절 신설(사용자 제안 — Step 1·2 채택, Step 3은 `corp_code` 불변으로 재배선 불필요 대체) | ✅ 해소 |
| Medium 6건 | TD-03(검증 리포트를 버전화까지 전달) · TD-04(error_review→**Rewrite 매핑**, 사용자 확정) · TD-05(발행을 접수 시점으로) · TD-06(classification 갱신) · TD-07(힌트 부재=1·2단계 공백 + rerank **재실행**으로 재정의) · TD-08(폐기 순서) | ✅ 해소 |
| Low 4건 | TD-09(최장 일치) · TD-10(run_all 계약) · TD-11(**무조건 합산 확정**, 사용자) · TD-12(검증 진입부 취소 확인) | ✅ 해소 |
| 사용자 확인 3건 | ① error_review→Rewrite ② 사유 유사성 조건 폐기 ③ Rewrite=Reject 동작 축약 유지 | ✅ 전부 확정 (2026-08-02) |

**판정: 해소 완료 — 구현 착수 가능.** 다음은 F 권장 순서의 B(Critical 3건 구현)부터.

---

## D. 남은 설계 확정 항목

| # | 항목 | 현재 상태 | 결정 필요한 것 |
|---|---|---|---|
| ~~D-8~~ | ~~O-14: 복수 소속 vs 모호성 구분~~ | ✅ **해결** | 판정 규칙이 아니라 **필드 분리**로 해결 — `company_industries`(기업 속성, OpenDART 조회)와 `document_industry`(문서 속성, 본문 판정). 문서 맥락 모호 시 **비워두는 것이 정상**, 두 필드 불일치는 **lint 탐지 신호**(A: AI 오판 / B: 신사업 진출 / C: 기업 식별 오류) |
| D-13 | **기업정보 지표 체계 반영** | ▶ 착수, 범위 확정 | 사용자 제공 자료를 **비상장기업 기준 공통 스키마**로 채택([12](12_기업정보_지표체계.md)). 발견: ① 법적 생명주기 vs 사업 생애주기는 **다른 축** ② ~~09 `Metrics` 카테고리 누락~~ → **5축 15종으로 해결**(O-40) ③ 자본금 계정 누락 → L1 19종에 반영 ④ 신용정보 → **Phase 1은 `CreditRiskSummary` 요약만, 이력은 Phase 2**(O-34) ⑤ 거래처 정보가 관계 그래프의 실제 재료 |
| ~~D-14~~ | ~~O-36-1: 산업분류 표 차수 검증~~ | ✅ **해결** (2026-07-26) | 통계청 『제11차 기준 KSIC 실무 적용 가이드북』(2025-01, hwpx) 원문 대조 완료. **중분류 코드 범위 전 항목 일치** → 기존 표는 10차가 아니라 **11차 계열**이었음. 어긋난 것은 명칭 표기뿐 — **H 섹션 "운수 및 창고업"→"운수업"** 1건(열거값도 `Transportation`으로 정정) + 띄어쓰기 4건. **마지막 구현 차단 항목 해제** |
| D-15 | **L1 계정 19종 + 가드레일 5종** | ✅ 확정, 구현 대기 | L1 **19종 확정**(요약 12종 역산 17종 → 원본 재검으로 현금흐름 3종 반영, `OperatingCashFlow` 폐기). 가드레일 5종 확정 — ⚠ **작동 지점이 3곳**: Guard 1·5는 저장 시, **Guard 2·3은 계산 시**(자본잠식은 잘못된 데이터가 아니라 정상 사실이므로 입력단에서 막으면 안 됨), Guard 4는 표시 시 ([09](09_재무수치_정의.md)) |
| D-16 | **원본 재검 반영** (신규) | ✅ 확정 | 참조 자료 트리 재확인으로 정정 4·발견 4건([12 §9](12_기업정보_지표체계.md)). 확정: **EBITDA 계열 유지**(현금영업이익으로 감가상각비 없이 확보, O-39) · **재무진단 5축 채택**(안정성→재무구조+부채상환능력 분리, 지표 15종, O-40) · **계속사업이익 L1 제외**(L2 플래그로 처리, O-41) · **매출비중 기반 주력산업 판정**(D-08 조치안 ④ 차단 해제, O-42) |
| ~~D-9~~ | ~~O-15: 매출 구간 미포괄~~ | ✅ **해결** | 하한(5억원 이하)·상한(1,500억원 초과) 추가 → **9구간이 전 범위 포괄**. `None`의 의미가 "매출액 정보 없음"으로 명확해짐 |
| ~~D-10~~ | ~~O-16: 업종 성격 ↔ 1단계 중복~~ | ✅ **해결** | H그룹을 6종 → **`TechIntensity` 2종(첨단/일반)** 으로 축소. 원래 6종은 "제조/서비스 × 첨단/일반" 조합이었고 **제조/서비스는 1단계 KSIC에서 이미 결정**되므로 3단계에 둘 이유가 없음. 두 값이 어긋날 가능성도 원천 차단 |
| D-11 | **O-17: 관련 기관의 관계 미정의** | ⏸ **보류** (사용자 결정: 관계는 나중에 정의) | 기관 56종은 목록만 보관하고 관계 정의는 미룬다. 정의 전까지는 검색·집계에 쓰지 않는다 |
| ~~D-12~~ | ~~O-18: 세목의 속성 대상~~ | ✅ **해결** | 세목 10종을 **기업 옵션에서 제외**하고 둘로 분리: ① **`document_tax_tags`**(문서 주제 분류 태그) — "국제조세 관련 문서만" 검색 가능 ② **세무 리스크·분쟁 이력은 Phase 2 리스크 영역**(12 §5 신용정보와 동일한 "시점 있는 사건" 성격). 근거: 목록에 **개인 세목**(종합소득·양도·상속증여)이 섞여 있어 "납부 세목" 해석이 성립 불가, 법인 납부분만 추리면 변별력 0. **O-14의 기업속성/문서속성 분리 구조를 재활용** |
| D-2 | **Research 스탠드얼론 판단 기준** | 판단 주체(이사장) 확정 / 기준("신규 원본자료 없음 + 순수 분석질의")은 **제안 상태** | 이사장이 무엇을 보고 분기할지 구체 기준 |
| ~~D-3~~ | ~~O-12: OCR 저신뢰 결과의 신뢰도 연동~~ | ✅ **해결** | `extraction_warnings`가 있으면 신뢰도를 **`Stated` → `Inferred` 자동 하향**([09 §2.6](09_재무수치_정의.md) Guard 0). 근거: L1은 "원본에 그대로 적힌 값"인데 OCR은 **그렇게 적혀 있는지 자체가 추정**이다. `rounded`·`converted`·`adjusted_tax_rate`와 같은 "손댄 흔적 남기기" 계열 |
| ~~D-4~~ | ~~O-13: 최신화 사전/사후 동시성~~ | ✅ **해결** | D-01 조치로 위험이 이미 크게 감소(LLM 호출 전부 직렬화). 남은 위험은 프리플라이트가 큐 밖에서 도는 구간뿐 → `PreflightIndex.wiki_version` 스냅샷 기록 후 **불일치 시 경고만, 재실행 안 함**. 프리플라이트는 참고용 우선순위이지 정합성 필수 데이터가 아니며, 되돌리면 동시 사용 시 계속 재시작하게 됨 |
| ~~D-5~~ | ~~O-1: 라우터 판정 규칙~~ | ✅ **해결** | **최신화·버전관리 에이전트에 흡수**(§8.2.1) — 별도 `router.rs` 만들지 않음. 프리플라이트가 순위를 내주므로 남는 일은 예산 배분뿐이고, **LLM 없이 알고리즘으로 처리**. `query()`도 프리플라이트 경유로 통일(§8.2.2, 시험 겸). Platform은 규칙 관리에서 **결과 관찰**로 역할 변경 |
| ~~D-6~~ | ~~O-3: 취소 경로~~ | ✅ **해결** | **요청 전체 취소**(파이프라인 통째 중단, 단계 진입부에서 확인). 진행 중 LLM 호출은 끊지 않되 다음 단계로 안 넘어감. 중간 산출물은 위키·draft 어디에도 안 남김. ⚠ **"SalesCS 유일 엔드포인트" 원칙의 예외 신설** — 취소는 SalesCS 도달 전에 발생하므로 이사장이 `request-cancelled` 이벤트를 직접 발행(웹+텔레그램 양쪽, **멈춘 단계까지 함께 통지**) ([설계서 §5.4](00_v0.1.5.1_설계서.md)) |
| ~~D-7~~ | ~~Platform 유지보수 주기~~ | ✅ **해결 — 2-Track** | **경량(앱시작+1일1회, LLM 없음)**: 통신로그 통계·미결작업 점검·라우터 결과 관찰 / **중량(주1회 또는 수동, LLM 사용)**: lint 스코프 점검. lint만 위키 전체를 모델에 태우므로 분리 → **비용 예측 가능**. 의존하던 4개 항목 스펙이 함께 닫힘 ([설계서 §5.3](00_v0.1.5.1_설계서.md)) |

> **O-2 소멸**: 기존 `related_ids` → `relations` 마이그레이션 방식(D1 §8 O-2)은 D-02 결정
> (기존 index.json 보존 불필요)으로 **불필요해져 자동 소멸**했다. 별도 조치 없음.

---

## E. 신규 포착 항목 (v0.1.5.2에서 처음 제기)

| # | 항목 | 설명 |
|---|---|---|
| E-1 | **논리오류 26종 프롬프트 공급** | ✅ **결정** — **전량 주입**으로 확정. 시간이 더 걸려도 전체 검출을 시도해 **데이터를 먼저 쌓는다**는 판단. 단 VRAM 제약(E-4)과 예산을 다투므로, 06 문서에 **프롬프트용 축약본**을 별도로 두어 판별 기준만 주입한다(완전판·프롬프트판·`FallacyType` 3자 동기화 필요). 실측 후 예산 초과 시 카테고리 5분할로 후퇴 검토(O-30) |
| E-2 | **3축 심각도 판정 신뢰성** | ✅ **결정** — **판정 근거를 함께 남긴다**. `SeverityAssessment`에 `rationale` 필드 추가(축별 판정 이유). 3축 판정도 결국 LLM 판단이라 결과만 남기면 오판을 사후 검증할 수 없기 때문 |
| E-3 | **파이프라인 LLM 호출 수** | ⏭ **구현 후 실측 예정** — 현재 판단할 데이터 없음. 구현 완료 후 샘플로 응답 시간 측정. 최소 경로: 전처리(OCR 시) → Taxonomy/Research → 검증 → 논리오류 검증 → 이사장 승인 → 최신화 → SalesCS. 재작업 3회 시 배수 증가, Map-Reduce 적용 시 청크 수만큼 추가 |
| E-4 | **VRAM 6GB 제약** | ✅ **가이드라인 확정** — ① `num_ctx` 단일 전역 설정 ② 하위 파라미터는 수식 파생(청크 = 20~25%) ③ **측정할 미지수를 `num_ctx` 상한 1개로 축소**. 프로토타입은 이 방향으로 진행. 상세: [11_컨텍스트_예산_진단.md](11_컨텍스트_예산_진단.md) |
| E-5 | **컨텍스트 예산 점검 방식** | ⏸ **툴 구현 보류** — 설계는 [11](11_컨텍스트_예산_진단.md)에 보존. 가이드라인(§1)·파생 수식(§4)만 적용하고 **임시값 `num_ctx=4096`이 당분간 운용값**. 재개 시 참고할 핵심: Ollama는 VRAM 부족 시 OOM이 아니라 **조용히 CPU 오프로드**하므로 성공/실패가 아니라 `/api/ps`의 `size_vram` vs `size`로 판정해야 함 |

---

## F. 권장 진행 순서

```
┌─ B. Critical 3건 구현 (D-01/D-02/D-03)  ← 즉시 착수 가능, 설계 확정 완료
│         │
│         ▼
│  C. High 5건 조치 (D-04~D-08)
│     └ D-04는 D-5(라우터 규칙)·E-1과 묶어서 함께
│
├─ D. 설계 확정 (D-1~D-7)  ← B와 병행 가능, 서로 독립적
│     └ D-7(유지보수 주기)은 다른 항목의 선행조건이라 먼저
│
└─ E. 신규 항목 검토 (E-1~E-3)
          │
          ▼
   전체 파이프라인 재워크스루
          │
          ▼
   회귀 검증 (03_결함기록부 §D-01/D-02/D-03 조치 확인)
```

### 재워크스루 시 중점 확인 항목

| 확인 | 근거 |
|---|---|
| 기존 `index.json`이 있는 상태에서 v0.1.5 최초 기동 → 크래시 없이 진행되는가 | D-02 회귀 |
| 이사장 → 팀 → 검증/논리오류 검증 전 구간 완주 (교착 없음) | D-01 회귀 |
| 질의 10회 반복 후 `index.json` 페이지 수 불변, `draft.json`만 증가 | D-03 회귀 |
| 논리오류 26종 중 실제로 검출되는 유형이 몇 종인지 | E-1 실효성 |
| 분석 1건당 총 LLM 호출 수와 소요 시간 실측 | E-3 |
