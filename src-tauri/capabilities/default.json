{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window -- grants core APIs (event listen/emit, window control) used by the Dart Wasm frontend for queue-update and telegram-event.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "shell:allow-open"
  ]
}
