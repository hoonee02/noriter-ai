name: noriter_ai_ui
description: Dart Wasm frontend for Noriter AI (Tauri webview). No dart:io -- talks to the Rust backend purely through Tauri IPC.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.4.0

dependencies:
  web: ^1.1.0

dev_dependencies:
  lints: ^4.0.0
